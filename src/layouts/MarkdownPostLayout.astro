---
import SidebarLayout from "./SidebarLayout.astro";
const { frontmatter } = Astro.props;
---

<SidebarLayout pageTitle={frontmatter.title} robots="index,follow" frontmatter={frontmatter}>
  <slot />
</SidebarLayout>
